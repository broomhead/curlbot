# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Docker
*.log

# League cache (regenerated at runtime)
league_cache.json
league_cache.json.tmp

# Subs board state (runtime; recreated on first /subs)
subs_store.json
subs_store.json.tmp

# Practice sign-up pool (runtime)
practice_signups.json
practice_signups.json.tmp

# macOS
.DS_Store
